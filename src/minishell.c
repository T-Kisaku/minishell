#include "minishell.h"
#include <stdio.h>
#include <stdlib.h>

int main() {
  puts("fire");
  puts("fire");
  /* t_data data; */
  /* data.lexered = NULL; */
  /* data.parsered = NULL; */
  /* data.lexered = lexer(argv); */
  /* data.parsered = (data.lexered); */
  /* executor(data.parsered); */
  /* cleanup(data); */
}
